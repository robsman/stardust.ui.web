package org.eclipse.stardust.ui.web.modeler.marshaling;

//import static org.hamcrest.CoreMatchers.is;
//import static org.junit.Assert.assertThat;

import org.junit.Test;

public class IdFromNameGenerationTest
{

   @Test
   public void test()
   {
      /*
      assertThat(deriveElementIdFromName("Hase"), is("HASE"));
      assertThat(deriveElementIdFromName("Hase!"), is("HASE"));
      assertThat(deriveElementIdFromName("Hase!!"), is("HASE"));
      assertThat(deriveElementIdFromName("!Hase"), is("HASE"));
      assertThat(deriveElementIdFromName("!!Hase"), is("HASE"));
      assertThat(deriveElementIdFromName(" Hase "), is("HASE"));

      assertThat(deriveElementIdFromName("Hase Blase"), is("HASE_BLASE"));
      assertThat(deriveElementIdFromName("Hase Blase!"), is("HASE_BLASE"));
      assertThat(deriveElementIdFromName("Hase Blase!!"), is("HASE_BLASE"));
      assertThat(deriveElementIdFromName(" Hase Blase "), is("HASE_BLASE"));

      assertThat(deriveElementIdFromName("Hase_Blase"), is("HASE_BLASE"));
      assertThat(deriveElementIdFromName("Hase__Blase"), is("HASE_BLASE"));
      assertThat(deriveElementIdFromName("Hase_$Blase"), is("HASE_BLASE"));

      assertThat(deriveElementIdFromName("Hase2Blase"), is("HASE2BLASE"));
      */
   }

}
